package:
  name: anaconda_project
  version: 0.1

build:
  entry_points:
    - anaconda-project = anaconda_project.commands.main:main

requirements:
  build:
    - python
    - setuptools
    - bcrypt

  run:
    - beautifulsoup4
    - ruamel_yaml
    - tornado
    - pycrypto
    - bcrypt
    - redis
